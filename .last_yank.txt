# set -as terminal-overrides ',*:Setulc=\E[58::2::%p1%{65536}%/%d::%p1%{256}%/%{255}%&%d::%p1%{255}%&%d%;m'  # underscore colours - needs tmux-3.0
background = "#232323"
C-down
C-k
# Bind Ctrl+J to navigate down the command history
bind -n C-j send-keys C-down
# Bind Ctrl+K to navigate up the command history
bind -n C-k send-keys C-up
n
p
-
d
a
d
C-j
bind -n C-j send-keys down
# Bind Ctrl+K to navigate up the command history
bind -n C-k send-keys up
# Bind Ctrl+J to navigate down the command history
bind -n C-j send-keys Down
# Bind Ctrl+K to navigate up the command history
bind -n C-k send-keys Up
-T
bind-key -T command_mode C-j command-prompt -p ':' "send-keys Up"
bind-key -T command_mode C-k command-prompt -p ':' "send-keys Down"
~
bind r
&> /tmp/tmux-open-in-nvim.log
tmux-open-in-nvim
open-in-vim.sh
# Now replace the last colon with a pipe
file=$(echo "$file" | rev | sed 's/:/'"|"'/' | rev)
echo $file

# Get file path, defaulting to ~/todo.md if no argument provided
raw_path="${1:-$HOME/todo.md}"

# Split on last colon and take everything before it
# Using rev to handle the last colon reliably
file=$(echo "$raw_path" | rev | cut -d':' -f2- | rev)

# If there were no colons, the above might return empty - fallback to raw_path
if [ -z "$file" ]; then
    file="$raw_path"
fi

# Now replace the last colon with a pipe
file=$(echo "$file" | rev | sed 's/:/'"|"'/' | rev)
echo $file
###########

